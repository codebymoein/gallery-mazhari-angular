# Security Policy

## Reporting a vulnerability

لطفاً آسیب‌پذیری‌های امنیتی را در Issue عمومی منتشر نکنید. از بخش **Security → Report a vulnerability** در همین مخزن استفاده کنید تا گزارش به‌صورت خصوصی برای مالک مخزن ارسال شود.

در گزارش، مسیر آسیب‌پذیر، روش بازتولید، اثر احتمالی و پیشنهاد اصلاح را درج کنید. اطلاعات حساس، داده کاربر یا کلیدهای واقعی را در گزارش قرار ندهید.

## Supported version

فقط آخرین نسخه شاخه اصلی پشتیبانی امنیتی می‌شود.

## Secrets

- فایل‌های `.env` نباید commit شوند.
- مقادیر موجود در `.env.example` صرفاً نمونه هستند.
- کلیدهای JWT، راه‌اندازی مدیر، SMTP، پرداخت و سرویس‌های پیام‌رسان باید در محیط استقرار نگهداری و دوره‌ای تعویض شوند.
