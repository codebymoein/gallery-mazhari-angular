# GM-028 — Remove unused booking service

Base: `main@ddf1b85388ae70825e561cd178c89b7fc136a97d`

## Scope
Remove only `src/app/core/services/booking.service.ts`, which calibrated Knip reports as an unused file and which has no repository references to `BookingService` or `simulateSmsReminder`.

## Non-scope
No changes to `NotificationService`, routes, templates, backend/API/database/data/media/SEO/deployment, or any other Knip finding.

## Verification
- Knip unused-file count should decrease from 10 to 9.
- All exact-head quality gates must remain green.

## Recovery
Revert this focused PR to restore the service; no runtime/data recovery is required.
